const numbers=document.querySelectorAll(".numbers"),operators=document.querySelectorAll(".operators");let currentOperand=document.querySelector(".current-operand"),historyOperand=document.querySelector(".previous-operand"),historyNumber="",currentNumber="";const numberArray=[0,0];numbers.forEach(((r,e)=>{r.addEventListener("click",(function(){currentNumber+=this.id,currentOperand.textContent=Math.trunc(currentNumber),"."===this.id&&(currentNumber+=this.value)}))})),operators.forEach((r=>{r.addEventListener("click",(function(r){""!=currentNumber&&(historyNumber=currentNumber,historyOperand.textContent=Math.trunc(historyNumber),currentNumber="",currentOperand.textContent="","-"===this.id&&console.log("event minus"))}))}));