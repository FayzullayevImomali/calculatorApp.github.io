const numbers=document.querySelectorAll(".numbers"),operators=document.querySelectorAll(".operators");let currentOperand=document.querySelector(".current-operand"),historyOperand=document.querySelector(".previous-operand"),historyNumber=0,currentNumber=0;const numberArray=[0,0];numbers.forEach(((r,e)=>{r.addEventListener("click",(function(){currentNumber+=this.id,currentOperand.textContent=Math.trunc(currentNumber)}))})),operators.forEach((r=>{r.addEventListener("click",(function(r){""!=currentNumber&&(historyNumber=currentNumber,historyOperand.textContent=Math.trunc(historyNumber),currentNumber="",currentOperand.textContent="","-"===this.id&&console.log("event minus"))}))}));